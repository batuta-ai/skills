# Plan — codex and claude adapters declare their qualified ACP bridges
<!-- inputs: profile.md@sha256:3f3de48a1e13 routing.md@sha256:556eb69f6375 -->

**Goal:** Let `--transport auto` reach the ACP bridges that core qualified on 2026-09-23 (core branch `feat/acp-bridge-qualification`, evidence in core `.batuta/acp-probes/qualify-2026-09-23/final/README.md`): the codex adapter declares `codex-acp` 1.13.1 in session mode `read-only`, the claude adapter declares `claude-agent-acp` 0.81.1 in mode `acceptEdits` with the sandbox session meta. The metadata lint accepts the two new keys and pins the exact values, as it already does for opencode.
**Created:** 2026-09-23 · **Status:** approved

## Tasks
- [ ] 1. The codex and claude adapters declare the ACP bridge metadata and the lint pins it — general/medium
      Scope: skills/batuta/adapters/codex.md, skills/batuta/adapters/claude.md, tests/skills/check.sh
      Accept: codex.md frontmatter declares acp_run `codex-acp`, acp_version `@agentclientprotocol/codex-acp 1.13.1`, acp_model_config `model` and acp_mode `read-only` → grep -qx 'acp_mode: read-only' skills/batuta/adapters/codex.md && grep -qx 'acp_version: @agentclientprotocol/codex-acp 1.13.1' skills/batuta/adapters/codex.md; claude.md frontmatter declares acp_run `claude-agent-acp`, acp_version `0.81.1`, acp_model_config `model`, acp_mode `acceptEdits` and acp_session_meta `{"claudeCode":{"options":{"sandbox":{"enabled":true,"autoAllowBashIfSandboxed":true}}}}` on one line → grep -qx 'acp_mode: acceptEdits' skills/batuta/adapters/claude.md && grep -qx 'acp_session_meta: {"claudeCode":{"options":{"sandbox":{"enabled":true,"autoAllowBashIfSandboxed":true}}}}' skills/batuta/adapters/claude.md; neither adapter declares acp_effort_config → ! grep -q '^acp_effort_config:' skills/batuta/adapters/codex.md skills/batuta/adapters/claude.md; each adapter body gains a short "ACP" paragraph saying what the mode and meta do (free inside the worktree, contained outside, pinned by the core qualification) → grep -q 'acp_mode' skills/batuta/adapters/codex.md && grep -q 'acp_session_meta' skills/batuta/adapters/claude.md; tests/skills/check.sh knows acp_mode and acp_session_meta as optional ACP keys, rejects acp_session_meta that is not a JSON object, and pins the exact codex, claude and opencode values in expected_acp → bash tests/skills/check.sh; the CLI run lines are unchanged → git diff --quiet origin/main -- skills/batuta/adapters/agy.md skills/batuta/adapters/cursor-agent.md skills/batuta/adapters/opencode.md

## Decisions and context

Conventional commits. Nothing else changes: CLI `run`, `run_file`, `readonly` and `model_flags` lines stay as they are, and routing is untouched.

**Task 1.** Core parses the frontmatter keys `acp_run`, `acp_version`, `acp_model_config`, `acp_effort_config`, `acp_mode` and `acp_session_meta`; a qualification matches only when the adapter's run, version, mode and compacted session meta equal the record exactly, so copy the values byte for byte. `acp_version` is the exact trimmed `--version` output of each bridge. Both bridges select effort by the session's `thought_level` category, so no `acp_effort_config` is declared (declaring one would stop the empty-effort qualification from matching). In `tests/skills/check.sh` (~95) extend `acp_keys` with the two new keys (they stay optional, like `acp_effort_config`), add a JSON-object check for `acp_session_meta` with `json.loads`, and add `codex` and `claude` entries to `expected_acp`. The claude ACP session advertises `opus[1m]`, `sonnet`, `haiku` and `default`, not `opus`; a route on `opus` is refused by the session before any prompt and `auto` falls back to CLI, which is expected.
